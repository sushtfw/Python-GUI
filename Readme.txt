In this file, I've used the tkinter module to make a GUI app which
lists and saves app into itself and can run all the saved apps with 
a single click. This only can list and open apps for now but I will
be updating it along time to remove and delete the apps too.